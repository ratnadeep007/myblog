import styled from 'styled-components';

const Chips = styled.div`
  background: rgb(202, 204, 209);
  border-radius: 10px;
  padding: 0.3rem;
  font-size: 0.8rem;
  display: flex;
  font-weight: 600;
  justify-content: center;
  &:first-child {
    margin: 0 0.6rem 0 0;
  }
`;

export default Chips;